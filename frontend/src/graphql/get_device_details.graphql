query GetDeviceDetails($id: Int!){
    device(id: $id){
        id
        name
        location{
            id
        }
        deviceType {
            name
            hasRouteros
        }
    }
}